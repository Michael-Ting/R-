1，缺失值是不能进行比较的，即使与缺失值自身也不能，所以不能用运算符检测人缺失值，可用is.na
2、R并不会把无限的，或者不可能出现的处理成缺失值，可以用is.infinite()或者is.nan()
3、含有缺失值的计算结果也是缺失值
4、多数的数值函数（sum、）都拥有一个na.rm=TRUE选项，可以在计算前移除缺失值后用剩余值计算
x<-c(1,2,3,na,5)
y<-sum(x,na.rm=TRUE)

可以通过函数na.omit（）移除所有含有缺失值的观测，也就是行
